[core]
    editor          = vim
[user]
    name            = Sudaraka Wijesinghe
    email           = sudaraka.wijesinghe@gmail.com
    signingkey      = D97F7914
[color]
    diff            = auto
    sh              = auto
    status          = auto
    branch          = auto
    ui              = true
[alias]
    l               = log --graph --decorate=short --oneline --all --date=short --format=format:'%C(bold red)%h%C(reset) %C(yellow)%ad%C(reset)%C(bold green)%d%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)'
    ll              = log --graph --decorate=short --stat
    dif             = diff --no-ext-diff
[push]
    default         = simple
[merge]
    tool            = splice
[mergetool]
    prompt          = false
[mergetool "splice"]
    cmd             = "gvim -f $BASE $LOCAL $REMOTE $MERGED -c 'SpliceInit'"
    trustExitCode   = true
[diff]
    external        = git_vimdiff.sh
[difftool]
    prompt          = false
[color "sh"]
    branch          = yellow
    dirty           = red bold
    dirty-stash     = red
    repo-state      = blue
    upstream-state  = cyan
