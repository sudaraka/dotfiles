[core]
    editor          = vim
[user]
    name            = Sudaraka Wijesinghe
    email           = sudaraka@sudaraka.org
    signingkey      = 14A657B6
[color]
    branch          = auto
    diff            = auto
    sh              = auto
    status          = auto
    ui              = true
[alias]
    a               = add
    ai              = add -i
    ap              = add -p
    b               = branch
    bl              = branch -vv
    c               = commit -e
    ca              = commit -ae
    ch              = checkout
    cp              = cherry-pick -x
    d               = diff --no-ext-diff
    ds              = diff --no-ext-diff --staged
    f               = fetch
    fa              = fetch --all
    fom             = fetch origin master
    l               = log --graph --decorate=short --oneline --all --date-order --format=format:'%C(green)%h%C(dim green) %G?%C(reset)%C(auto)%d%C(reset) %C(white)%s%C(reset)'
    ll              = log --graph --decorate=short --oneline --all --date-order --date=short --format=format:'%C(green)%h%C(dim green) %G?%C(reset) %C(yellow)%ad%C(reset)%C(auto)%d%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)'
    lx              = log --graph --decorate=short --stat --date-order --show-signature
    m               = merge --no-ff
    mff             = merge --ff-only
    ph              = push
    pl              = pull
    r               = reset
    rb              = rebase
    rh              = reset --hard
    rv              = revert
    s               = status -sb
    sa              = stash apply
    sc              = stash clear
    sl              = stash list --format=format:'%C(blue)%gd%C(reset) %C(white)%s%C(reset)'
    sp              = stash pop
    ss              = stash save
[push]
    default         = simple
    followTags      = true
[diff]
    external        = git_vimdiff.sh
[difftool]
    prompt          = false
[rebase]
    autosquash      = true
    autostash       = true
[commit]
    gpgsign         = true
